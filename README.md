# tunnel-probe

The test spoke of the WireGuard tunnel: a devkit-managed container in fetched mode that proves enrolment, the bundle fetch and the handshake with the hub end to end, outside production. Design: `docs/superpowers/specs/2026-09-14-hub-fetched-peer-config-design.md`, sections 10.1 and 11. Its database query is the next phase.
